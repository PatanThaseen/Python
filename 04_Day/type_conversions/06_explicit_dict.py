#dict
d = {1: "A", 2: "B", 3: "C"}
print(tuple(d))#only keys will be the output 
print(str(d))#only keys will be the output 
#dict to list

print(list(d))#only keys will be the output 

print(set(d))#only keys will be the output 
